version: "3.9"
services:
  resume-matcher:
    build: .
    container_name: resume-matcher-fullstack
    ports:
      - "80:80"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped